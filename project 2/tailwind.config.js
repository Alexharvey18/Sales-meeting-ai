/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        blue: {
          400: 'rgb(var(--color-salesforce-cloud) / <alpha-value>)',
          600: 'rgb(var(--color-salesforce-blue) / <alpha-value>)',
        },
        green: {
          600: 'rgb(var(--color-salesforce-green) / <alpha-value>)',
        },
      },
      animation: {
        fadeIn: 'fadeIn 0.3s ease-in-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: 0, transform: 'translateY(-10px)' },
          '100%': { opacity: 1, transform: 'translateY(0)' },
        },
      },
    },
  },
  plugins: [],
};